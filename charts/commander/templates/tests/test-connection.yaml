apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "commander.fullname" . }}-test-connection"
  labels:
    {{- include "commander.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: resolv-conf
      image: busybox
      command: ['cat']
      args: ['/etc/resolv.conf']
    - name: nc
      image: busybox
      command: ['nc']
      args: ['-v', '-z', '{{ include "commander.fullname" . }}', '{{ .Values.service.port }}']
    - name: curl
      image: curlimages/curl
      args: ['-LIX', 'GET', '{{ include "commander.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
